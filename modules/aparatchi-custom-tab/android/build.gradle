apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.aparatchicustomtab'
version = '1.0.0'

expoModule {
  canBePublished false
}

android {
  namespace 'expo.modules.aparatchicustomtab'

  defaultConfig {
    versionCode 1
    versionName '1.0.0'
  }
}

dependencies {
  implementation 'androidx.browser:browser:1.9.0'
}
