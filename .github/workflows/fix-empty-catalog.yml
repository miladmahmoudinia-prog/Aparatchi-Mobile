name: Fix empty catalog on first launch

on:
  push:
    branches: [main]
    paths:
      - 'scripts/fix-empty-catalog.mjs'
      - '.github/workflows/fix-empty-catalog.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: aparatchi-mobile-source-fix
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 20
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
      - name: Apply empty-catalog recovery
        run: node scripts/fix-empty-catalog.mjs
      - name: Install dependencies
        run: npm ci
      - name: Type-check application
        run: npm run typecheck
      - name: Commit verified recovery
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add App.tsx src/contentService.ts package.json app.json
          if git diff --cached --quiet; then
            echo "Empty-catalog recovery already applied."
            exit 0
          fi
          git commit -m "fix: keep catalog visible while remote refresh loads"
          PUSHED=0
          for attempt in 1 2 3 4 5; do
            if git pull --rebase origin main && git push origin HEAD:main; then PUSHED=1; break; fi
            git rebase --abort >/dev/null 2>&1 || true
            sleep $((attempt * 2))
          done
          [ "$PUSHED" -eq 1 ] || exit 1
