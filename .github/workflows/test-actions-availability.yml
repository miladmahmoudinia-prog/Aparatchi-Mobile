name: Test Actions availability

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-actions-availability.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Mobile Actions runner is available"
