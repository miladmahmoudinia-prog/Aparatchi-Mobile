name: Apply user report v11 mobile fixes

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-user-report-v11.mjs'
      - 'scripts/verify-user-report-v11.mjs'
      - '.github/workflows/apply-user-report-v11.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: aparatchi-mobile-user-report-v11
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout mobile repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 20

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Apply audited mobile fixes
        run: |
          node scripts/apply-user-report-v11.mjs
          node scripts/verify-user-report-v11.mjs

      - name: Install JS dependencies
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Commit verified mobile fixes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "aparatchi-mobile-bot"
          git config user.email "actions@users.noreply.github.com"
          git add App.tsx src/contentService.ts
          if git diff --cached --quiet; then
            echo "No source changes to commit."
            exit 0
          fi
          git commit -m "fix: fresh startup detail truth and navigation v11"
          for attempt in 1 2 3 4 5; do
            if git pull --rebase origin main && git push origin HEAD:main; then
              exit 0
            fi
            git rebase --abort >/dev/null 2>&1 || true
            sleep $((attempt * 2))
          done
          echo "::error::Could not push v11 mobile fixes after 5 attempts."
          exit 1

# trigger: helpers preserved and regional guards added
